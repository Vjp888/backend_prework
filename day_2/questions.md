## Day 2 Questions

1. Create an array containing the following strings: `"zebra", "giraffe", "elephant"`.
["zebra", "giraffe", "elephant"]

1. Save the array you created above to a variable `animals`.
animals = ["zebra", "giraffe", "elephant"]

1. using the array `animals`, how would you access `"giraffe"`?
animals[1]

1. How would you add `"lion"` to the `animals` array?
animal << "lion"

1. Name and describe two additional array methods.
.slice! will remove a given point from the arrays
.each will go through the array and print out each on on a new line.

1. What are the boolean values in Ruby?
They are asking for a true/false statement by using "Truth Terms"
Such statements include
&& (And), || (or), ! (Not), == (equal) and so on.

1. In Ruby, how would you evaluate if `2` is equal to `25`? What is the result of this evaluation?
2 == 25
it would come back as false.

1. In Ruby, how would you evaluate if `25` is greater than `2`? What is the result of this evaluation?
25 > 2
It would come back as true.
